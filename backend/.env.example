# MongoDB Atlas connection string (replace with your own credentials)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/career_compass

# Server port (optional, defaults to 5000)
PORT=5000

# Optional: Google Gemini API key for external cloud LLM responses
# GEMINI_API_KEY=your_gemini_api_key_here
